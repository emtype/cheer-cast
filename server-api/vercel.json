{
  "version": 2,
  "buildCommand": "cd client && rm -rf build node_modules/.cache && yarn install && yarn build",
  "outputDirectory": "client/build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}